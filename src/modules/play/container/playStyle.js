const playStyle = (theme) => ({
  root: {
    flexGrow: 1,
    top: '0',
    height: '100vh',
    position: 'relative',
  },
  container: {
    flexGrow: 1,
    padding: theme.spacing(3),
    display: 'flex',
    justifyContent: 'center',
  },
});

export default playStyle;
